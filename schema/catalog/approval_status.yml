table: approval_status
comment: Lifecycle of one runtime approval request
columns:
  - name: code
    type: VARCHAR(30)
  - name: is_terminal
    type: BOOLEAN
  - name: description
    type: VARCHAR(200)
primary_key: [code]
